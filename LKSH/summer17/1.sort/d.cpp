#include <iostream>
#include <vector>
#include <algorithm>
#include <string>


bool StrComparator(std::string first, std::string second) {
    return !(first > second);
}

int main() {
    std::vector<std::string> numbers;
    while (read) {
        std::string elem;
        std::cin >> elem;
        numbers.push_back(elem);
    }

    std::sort(teams.begin(), teams.end(), StrComparator);
    for (std::string number : numbers) {
        std::cout << cumber;
    }
    std::cout << '\n';

    return 0;
}