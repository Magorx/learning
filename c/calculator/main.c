#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <string.h>
#include "general.h"
#include "calculator.h"

#define ARR_LEN(arr): sizeof(arr) / sizeof(arr[0]);
int main() {
    struct token *token_arr = tokenize("1+15*(35-11)");
    for (int i = 0; i < )

    return 0;
}